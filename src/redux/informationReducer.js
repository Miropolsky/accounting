const initialState = {
    given: 30,
    onMountain: 28,
    onSurface: 2,
    violators: 0,
};
const informationReducer = (state = initialState, action) => {
    return state;
};

export { informationReducer };
